# Rust Shell

Rust toolchain development environment.

## Packages

- `rustup`, `cargo`, `rustfmt`, `clippy`
- `rust-analyzer`, `lldb`, `gdb`

## Usage

```bash
nix develop .#rust
rustup show
cargo new my-project
```