# Node.js Shell

Node.js/JavaScript development environment.

## Packages

- `nodejs_20`, `bun`, `pnpm`
- `eslint`, `prettier`, `typescript`
- `ts-node`, `just`

## Usage

```bash
nix develop .#nodejs
node --version
bun --version
pnpm --version
```